<?xml version="1.0"?>
<fragment version="1">

 <require>all</require>

  <install>
    <!-- Unzip the contents of our web application into the server -->
    <unzip from="${bundle.fileName}" to="/" prefix="web">
      <include>web/**</include>
    </unzip>
  </install>

</fragment>
